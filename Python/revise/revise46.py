# Python program to check if a rectangle is a square
length = float(input("Enter the length: "))

breadth = float(input("Enter the breadth: "))

if length == breadth:

    print("It is a square.")

else:
    
    print("It is not a square.")
